web: export HTTP_PROXY=$PROXYHOST:$PROXYPORT && export HTTPS_PROXY=$PROXYHOST:$PROXYPORT && python runserver.py -a "$AUTH_SERVICE" -u "$USERNAME" -p "$PASSWORD" -l "$LOCATION" -st $STEP_COUNT -H 0.0.0.0 -P $PORT -k $GMAPS_KEY -hk "$HOST_PASSKEY" -ng -nk -sd 20 $EXTRA_ARGS
